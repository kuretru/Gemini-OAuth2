server:
  port: 7051
  servlet:
    context-path: /api
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${mysql.username:gemini}
    password: ${mysql.password:123456}
    url: jdbc:mysql://${mysql.hostname:127.0.0.1}:${mysql.port:3306}/${mysql.schema:gemini}?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
  redis:
    host: ${redis.host:127.0.0.1}
    password: ${redis.password:}
    port: ${redis.port:6379}
    database: ${redis.database:5}
